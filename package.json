{
  "name": "mathic",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test && node tests/playtest-puzzle.mjs",
    "serve:https": "node scripts/https-server.mjs",
    "audit:size": "node scripts/audit-size.mjs",
    "gen:icons": "node scripts/gen-icons.mjs"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@wllama/wllama": "^3.6.1"
  }
}
