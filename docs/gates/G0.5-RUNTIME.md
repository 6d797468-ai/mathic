# G0.5-RUNTIME — Rapport de gate (format §13)

```
TEAM:     Kali
PROJECT:  MATHIC V4
GATE:     G0.5 — Runtime Feasibility
BASELINE: 09a45a38f0fd963494d09ca65de5d6c161022285
BRANCH:   kali/g0.5-runtime
PARENT:   09a45a3
```

> **DÉROGATION DOCUMENTÉE (D8)** : G0 n'était pas encore intégré à `main` au démarrage de ce gate (merge annoncé non prouvé — triple vérification API GitHub : 0 PR sur le dépôt, `main` inchangée, aucun SHA-MAIN-1 fourni). Sur décision explicite de l'exploitant, G0.5 démarre depuis la baseline `09a45a3` et est isolé sur `kali/g0.5-runtime`. **Aucun ordre d'intégration n'est changé** : `kali/g0` reste la première PR à merger ; cette branche sera re-basée si l'architecte l'exige. La sonde modifie le code produit de zéro façon.

## OBJECTIVE
Déterminer si le runtime actuel est techniquement viable pour la suite V4 — mesurer, ne pas améliorer.

## CLAIMS
- Le build de production fonctionne et produit un bundle mesurable.
- Le serveur origin HTTPS local est viable (TLS auto-signé, non-commité).
- La couche HTTP Range de l'origin (prérequis du GGUF streaming) est fonctionnelle et intègre (SHA-256 vérifié).
- Le core pur tourne hors DOM (createBoard/fillInitialTiles/slideBoard) et est rapide.
- La dette D7 (BFS sur thread UI) est réelle et désormais chiffrée.

## PROVEN
| Domaine | Preuve |
|---|---|
| BUILD | `vite build` OK — 3,9 s · bundle `assets/index-BDNiOvS0.js` 354,4 kB (fichier vérifié au disque) |
| SERVER | HTTPS origin :8443 — `index.html` 200 via TLS auto-signé (`certs/` présents, jamais commités) |
| RANGE-A (origin) | 4 plages testées : `206` · `Content-Range: bytes 0-524287/88201568` · **SHA-256 du contenu reconstitué = attendu** sur chaque plage · overlap → 206 conforme · hors-limites → `416` propre |
| GAME (core pur) | `createBoard`+`fillInitialTiles` : 8 tuiles, entiers ≥ 1 · 1000 coups `slideBoard` en 55 ms (~0,055 ms/coup) |
| PERF-BFS (dette D7) | `minMovesToReach` depth 4 : min 6,1 · moy 19,7 · max 43,2 ms (Node v24.19.0) — **mesuré, pas supposé** |
| CI externe (observation) | Runs `test.yml` de lkaddafi verts sur leurs 3 branches ; `build-android` success sur `09a45a3` ; **`deploy-pages` FAILURE sur `09a45a3`** (fait majeur consigné, hors périmètre Kali) |

## NOT PROVEN
- Déterminisme cross-platform (aucune affirmation de ce type n'est déduite des 1000 coups — règle §8).
- Pages opérationnelles (workflow en échec sur la baseline — ne pas confondre « workflow existant » et « déploiement qui marche »).

## BLOCKED
- Création de la PR `kali/g0 → main` (aucun jeton API dans l'environnement — clic opérateur requis ; pas de SHA/URL de merge fourni après annonce).

## FAIL
- Aucun FAIL mesuré dans le périmètre couvert (11 PASS · 0 FAIL à la dernière exécution).

## NOT TESTABLE (HERE)
| Item | Motif |
|---|---|
| SW actif / caches / offline reload / Range via Service Worker (couche B) | Navigateur requis — aucun Chrome/Chromium sur kali |
| Rendu, interactions, erreurs console | Navigateur requis |
| Android WebView + APK install | SDK Android absent (`$ANDROID_HOME` inexistant) — les commandes ne sont pas inventées |
| Worker solver — exécution réelle | Worker navigateur requis ; worker_threads Node ≠ runtime cible. Un test structurel ne serait pas une preuve (§8) |

## FILES CREATED
- `tests/runtime/g05-probe.mjs` (sonde, zéro dépendance, `NOT TESTABLE` explicite pour ce qui n'est pas mesurable ici)
- `docs/gates/G0.5-RUNTIME.md` (ce rapport)
- `docs/evidence/G0.5-runtime-kali.json` (preuve machine : 11 résultats horodatés + liste notTestableHere)

## FILES MODIFIED
- Aucun fichier de code produit (`src/**`, `public/**`, `index.html`, `package.json` intacts — `git diff --stat` fera foi)

## FILES DELETED
- Aucun

## TESTS
- `node tests/runtime/g05-probe.mjs` → **11 PASS · 0 FAIL · 0 NOT TESTABLE** (exit 0) — preuve JSON écrite
- Socle inchangé : `node --test` (logic/diff/history) 3/3 sur cette branche (identique à la baseline)

## BUILD
- `npx vite build` → succès, 3,9 s · 354,4 kB JS (+ CSS), exécuté par la sonde elle-même

## RUNTIME
- environment : kali (Linux, Node v24.19.0) · serveur origin HTTPS :8443 · résultat : voir PROVEN

## CI
- workflow : aucun nouveau (G0.5 n'écrit pas de CI — c'est G0.6) · observation : runs lkaddafi + historique Pages/Android consignés

## KNOWN DEBT
- D7 désormais **chiffrée** : BFS 6–43 ms sur UI thread (G10)
- D1–D4, D5, D6, D8, D9, D10, D11 inchangées (consignées dans `MATHIC-V3-BASELINE.json`)

## NON-GOALS RESPECTED
- Aucun refacto `main.js`, aucun déplacement de module, aucune modification board/puzzle/ai/targets, aucun PRNG, aucun G1/G2/G3, aucun niveau V4, aucune modification Momo ni des contrats. La sonde est du outillage de test, documentée comme telle.

## EVIDENCE
- Sonde : `docs/evidence/G0.5-runtime-kali.json` (11 entrées horodatées, compteur buildMs)
- Sortie console de la dernière exécution (transmise au fil de l'eau)
- Extrait clé : `Content-Range: bytes 0-524287/88201568` + SHA-256 contenus vérifiés
- CI externe observée : `test`×4 success (branches lkaddafi) · `build-android` success · **`deploy-pages` failure** sur `09a45a3`

## FINAL VERDICT
**PROVEN (périmètre couvert)** — le runtime origin est viable pour la suite V4 dans les limites ci-dessus ; G0.5 partiel par nature (couches navigateur/Android/Worker → NOT TESTABLE HERE, à couvrir par Playwright en G0.6+ ou sur machine équipée).
