# G0.6 — CI Hardening Evidence

> Gate : G0.6 — Status : **IN_PROGRESS**

## G0.6.1 — test.yml

- [x] Fichier créé : `.github/workflows/test.yml`
- [x] Déclenché sur : `push` (toutes branches) + `pull_request`
- [x] Étapes : `npm ci` → `npm test` → `npm run build` → vérif SW VERSION
- [ ] CI run vert ← **à confirmer après push**

Lien run CI : _(à renseigner)_

## G0.6.2 — pages.yml

- [x] `npm test` ajouté avant le build
- [x] Vérification SW VERSION ajoutée après build
- [ ] Déploiement Pages confirmé ← **vérifier que Pages est activé en mode GitHub Actions**

Lien déploiement : _(à renseigner)_

## G0.6.3 — android-build.yml

- [x] `npm test` ajouté avant `npm run build:android`
- [ ] APK artifact généré et téléchargeable ← **à confirmer après push**

Lien artifact : _(à renseigner)_

## G0.6.4 — SW Version Evidence

Post-build, la commande suivante doit retourner une valeur concrète :
```bash
grep "const VERSION" dist/sw.js
# Attendu : const VERSION = 'mathic-v4-09a45a3-XXXXXXXX';
# Jamais  : const VERSION = '__SW_VERSION__';
```

Résultat observé : _(à renseigner après build)_

## Gate G0.6 PASS =

```
[ ] test.yml — run CI vert (lien)
[ ] pages.yml — déploiement OK (lien)
[ ] android-build.yml — APK artifact OK (lien)
[ ] dist/sw.js — VERSION concrète (grep output)
```
